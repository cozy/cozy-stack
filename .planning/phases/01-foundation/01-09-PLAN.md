---
phase: 01-foundation
plan: 09
type: execute
wave: 5
depends_on: ["01-07", "01-08"]
files_modified:
  - web/webdav/gowebdav_integration_test.go
autonomous: true
requirements:
  - TEST-04
  - SEC-05
must_haves:
  truths:
    - "A gowebdav.Client configured with Basic Auth (empty username, token password) can ReadDir the root and see seeded files"
    - "gowebdav.Client.Stat on a seeded file returns the correct size and mod time"
    - "gowebdav.Client.Read on a seeded file returns the exact bytes"
    - "Every successful PROPFIND and GET response has a Content-Length header (SEC-05) OR uses Transfer-Encoding: chunked for streamed PROPFIND Depth:1"
    - "All 5 Phase 1 success criteria from ROADMAP.md are observably true via this test"
  artifacts:
    - path: "web/webdav/gowebdav_integration_test.go"
      provides: "End-to-end integration test exercising the full stack via the gowebdav client and asserting every Phase 1 success criterion"
  key_links:
    - from: "web/webdav/gowebdav_integration_test.go"
      to: "web/webdav.Routes and the full middleware chain"
      via: "newWebdavTestEnv + gowebdav.NewAuthClient"
      pattern: "gowebdav\\.NewAuthClient|newWebdavTestEnv"
---

<objective>
Wave 5 — Final gate: Drive the complete WebDAV read-only surface from a real gowebdav client against the test instance and verify every Phase 1 success criterion from ROADMAP.md. This is the "would an actual WebDAV client work?" test — the most authoritative signal before /gsd:verify-work.

Purpose: TEST-04 (integration auth with a real WebDAV client), SEC-05 (Content-Length sanity check on the full surface), plus observable verification of all 5 ROADMAP success criteria.
Output: gowebdav_integration_test.go GREEN; handoff summary confirming Phase 1 is ready for verification.
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-VALIDATION.md
@web/webdav/webdav.go
@web/webdav/testutil_test.go
@.planning/phases/01-foundation/01-06-SUMMARY.md
@.planning/phases/01-foundation/01-07-SUMMARY.md
@.planning/phases/01-foundation/01-08-SUMMARY.md

<interfaces>
gowebdav v0.12.0 client API:
```go
import "github.com/studio-b12/gowebdav"

c := gowebdav.NewAuthClient(url, gowebdav.NewPreemptiveAuth(gowebdav.NewBasicAuth("", token)))
// or: c := gowebdav.NewClient(url, "", token) // username ignored, token in password
err := c.Connect()           // optional sanity check
infos, err := c.ReadDir("/") // PROPFIND Depth:1 under the hood
info, err  := c.Stat("/hello.txt")
data, err  := c.Read("/hello.txt")
```

Phase 1 success criteria (from ROADMAP.md):
1. Valid Bearer token client can PROPFIND Depth:0/1 and get all 9 live properties with D: namespace prefix.
2. Unauthenticated non-OPTIONS → 401 WWW-Authenticate Basic realm="Cozy"; OPTIONS returns DAV: 1 and Allow list without auth.
3. Depth:infinity → 403. Path traversal (../, %2e%2e, null bytes, /settings prefix) rejected before VFS call.
4. GET on file streams with Content-Length, ETag, Last-Modified; Range works; GET on collection → 405.
5. /remote.php/webdav/* → 308 redirect to /dav/files/*; subsequent request succeeds.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — add end-to-end gowebdav integration test (initially failing if any earlier wave broke a criterion)</name>
  <files>web/webdav/gowebdav_integration_test.go</files>
  <read_first>
    - .planning/ROADMAP.md (the 5 Phase 1 success criteria)
    - web/webdav/testutil_test.go (newWebdavTestEnv + any seedFile helper added in plan 08)
    - web/webdav/propfind_test.go (how files are seeded via env.Inst.VFS())
    - web/webdav/get_test.go (same)
  </read_first>
  <behavior>
    A single consolidated test (optionally split into 5 subtests matching the 5 criteria):

    - `TestE2E_GowebdavClient` with subtests:
      1. `SuccessCriterion1_BrowseWithBearerToken` — Seed files and a directory. Use gowebdav client with Basic auth (empty username, token password — works per research). ReadDir("/") returns the seeded entries. Stat on a file returns correct size. Read returns correct bytes.
      2. `SuccessCriterion2_AuthRequiredExceptOptions` — Raw HTTP: PROPFIND without Authorization → 401 + `WWW-Authenticate: Basic realm="Cozy"`. OPTIONS without Authorization → 200 + `DAV: 1` + Allow containing OPTIONS/PROPFIND/GET/HEAD.
      3. `SuccessCriterion3_SecurityGuards` — Raw HTTP: PROPFIND with `Depth: infinity` → 403. Path traversal (`/dav/files/..%2fsettings`) → 403. Each should leave the VFS untouched.
      4. `SuccessCriterion4_GetFileAndCollection` — GET file with Range header → 206 partial content. GET collection → 405 with Allow header. HEAD file → 200 with Content-Length + ETag, no body.
      5. `SuccessCriterion5_NextcloudRedirect` — Raw HTTP PROPFIND to `/remote.php/webdav/` → 308 with `Location` pointing at `/dav/files/`. Follow the redirect manually and assert the second request succeeds (this simulates an HTTP client that honours 308).

    The test uses the standard `newWebdavTestEnv(t, nil)` helper so it exercises the fully wired Routes.
  </behavior>
  <action>
    Create web/webdav/gowebdav_integration_test.go.

    Guard with the standard short-skip.

    Imports: `github.com/studio-b12/gowebdav`, `github.com/stretchr/testify/assert`, `github.com/stretchr/testify/require`, testify, stdlib.

    For the gowebdav client, Cozy's auth pattern is:
    ```go
    c := gowebdav.NewClient(env.TS.URL+"/dav/files", "", env.Token)
    require.NoError(t, c.Connect())
    infos, err := c.ReadDir("/")
    ```

    For the raw PROPFIND, OPTIONS, and redirect tests, use env.E (httpexpect) with `.Request("PROPFIND", ...)`, `.OPTIONS(...)`, etc. Disable automatic redirect following on httpexpect so you can observe the 308 response directly — httpexpect exposes this via WithRedirectPolicy(httpexpect.DontFollowRedirects).

    Run: `go test ./web/webdav/ -run TestE2E -count=1 -v`

    Expected result: tests PASS because all earlier waves have already made the individual pieces green. If anything fails, it reveals an interaction bug between plans — fix the underlying plan's code (not this test) and mark in the summary.

    Commit: `test(webdav): add end-to-end gowebdav integration test covering Phase 1 success criteria`
  </action>
  <verify>
    <automated>go test ./web/webdav/ -run TestE2E -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./web/webdav/ -run TestE2E -count=1` exits 0
    - `grep -c 'SuccessCriterion' web/webdav/gowebdav_integration_test.go` shows at least 5
    - `grep -q 'studio-b12/gowebdav' web/webdav/gowebdav_integration_test.go`
    - git log shows commit matching `test(webdav): .*end-to-end.*gowebdav`
  </acceptance_criteria>
  <done>End-to-end test green.</done>
</task>

<task type="auto">
  <name>Task 2: Full package run with -race and Phase 1 verification</name>
  <files>web/webdav/ (read-only check)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 success criteria)
    - .planning/phases/01-foundation/01-VALIDATION.md (per-task map)
  </read_first>
  <action>
    1. Run the full race-enabled suite:
       ```
       go test ./web/webdav/... -race -count=1 -timeout 5m
       ```
       MUST exit 0.

    2. Run go vet:
       ```
       go vet ./web/webdav/...
       ```
       MUST be clean.

    3. Run the broader build sanity check:
       ```
       go build ./...
       ```
       MUST exit 0.

    4. Update `.planning/phases/01-foundation/01-VALIDATION.md`:
       - Mark each row in "Per-Task Verification Map" with the real task ID from the committed plans (01-01 through 01-09) and status ✅ green.
       - Flip `nyquist_compliant: true` and `wave_0_complete: true` in the frontmatter.
       - Set the Approval line to `approved`.

    5. No code changes in this task. If any step 1-3 fails, DO NOT paper over it — file the gap as an explicit TODO, commit the VALIDATION.md update with `nyquist_compliant: false` and a gap note, and return a CHECKPOINT instead of success.

    Commit: `docs(webdav): finalize phase-01 validation map — all tasks green`
  </action>
  <verify>
    <automated>go test ./web/webdav/... -race -count=1 -timeout 5m &amp;&amp; go vet ./web/webdav/... &amp;&amp; go build ./... &amp;&amp; grep -q 'nyquist_compliant: true' .planning/phases/01-foundation/01-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Full suite passes with `-race`
    - `go vet ./web/webdav/...` clean
    - `go build ./...` exits 0
    - VALIDATION.md has `nyquist_compliant: true` and real task IDs in the per-task table
    - git log shows commit matching `docs(webdav): finalize phase-01 validation`
  </acceptance_criteria>
  <done>Phase 1 verified, VALIDATION.md marked approved.</done>
</task>

</tasks>

<verification>
- Full race-enabled test suite passes
- All 5 Phase 1 ROADMAP success criteria are observably verified by TestE2E_GowebdavClient subtests
- VALIDATION.md shows nyquist_compliant: true
</verification>

<success_criteria>
- End-to-end gowebdav test passes
- Full package runs clean with -race
- VALIDATION.md finalized
- Two commits: e2e test, validation update
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-09-SUMMARY.md` with:
- All 5 ROADMAP success criteria and which subtest verifies each
- Full package test run summary (counts, race results)
- Handoff to `/gsd:verify-work` for Phase 1 sign-off
</output>
