---
phase: 04-prerequisites-and-instrumentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/webdav/testutil_test.go
autonomous: true
requirements: [DEBT-02]
must_haves:
  truths:
    - "newWebdavTestEnv can be called with a *testing.B without compilation error"
    - "All existing *testing.T callers continue to compile and pass"
    - "Running `go test -short ./web/webdav/...` without COZY_COUCHDB_URL skips integration tests via NeedCouchdb (not via the blanket testing.Short skip, which is removed)"
  artifacts:
    - path: web/webdav/testutil_test.go
      provides: "testing.TB-widened newWebdavTestEnv; blanket testing.Short() skip removed"
      contains: "func newWebdavTestEnv(tb testing.TB"
  key_links:
    - from: web/webdav/testutil_test.go
      to: web/webdav/*_test.go (13 existing callers)
      via: "testing.TB is the superset interface of *testing.T and *testing.B; no caller change needed"
      pattern: "newWebdavTestEnv\\(t,"
---

<objective>
Land DEBT-02 (widen `newWebdavTestEnv` signature from `*testing.T` to `testing.TB`) and remove the blanket `testing.Short()` skip from `testutil_test.go:35`. Both changes are in the same file (one file, two micro-edits) and must happen BEFORE any INSTR helper is written so those helpers are built against `testing.TB` from the start.

Purpose:
- DEBT-02: enables Phase 5 benchmarks (`*testing.B`) to use the same environment setup as tests.
- Blanket skip removal: unblocks Phase 8 CI-03 — the `-short` flag in `go-tests.yml` must skip only the heavy LARGE/CONC tests (which opt in via their own `testing.Short()` guard), not every WebDAV integration test. `NeedCouchdb(t)` on line 42 is already the proper guard for the "no infra" case.

Output: one file modified, 13 callers untouched (because `testing.TB` is a superset interface), all existing tests still compile and pass.
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-prerequisites-and-instrumentation/04-CONTEXT.md

<interfaces>
<!-- Current testutil_test.go signature and the two spots touched by this plan -->

Current state (67 lines, relevant portion):
```go
// Line 33-36 — signature + blanket skip to remove:
func newWebdavTestEnv(t *testing.T, overrideRoutes func(*echo.Group)) *webdavTestEnv {
    t.Helper()
    if testing.Short() {                                                          // line 35 — REMOVE (with line 36)
        t.Skip("webdav integration tests require a cozy test instance")           // line 36 — REMOVE
    }
    if overrideRoutes == nil {
        overrideRoutes = Routes
    }
    config.UseTestFile(t)
    testutils.NeedCouchdb(t)   // line 42 — this is the proper guard, KEEP unchanged

    setup := testutils.NewSetup(t, t.Name())  // uses t; must adapt to tb
    // ...
}
```

Signature change: `t *testing.T` → `tb testing.TB`. The `testing.TB` interface includes:
- `Helper()`, `Skip(...)`, `Log(...)`, `Logf(...)`, `Error(...)`, `Errorf(...)`, `Fatal(...)`, `Fatalf(...)`, `Name()`, `Cleanup(func())`, `TempDir()`, `Setenv(string, string)` etc.
- Every method used by the function body (`t.Helper`, `t.Name`, `t.TempDir`, `t.Cleanup`, `t.Setenv`) is present on `testing.TB`.

Downstream concerns:
- `config.UseTestFile(t)` — signature is `func UseTestFile(t testing.TB)` (already accepts TB; verify on read — if it only takes `*testing.T`, the parameter remains compatible because `*testing.T` implements `testing.TB`, but we need to pass a `testing.TB` value; if UseTestFile strictly requires `*testing.T`, widening may fail — INVESTIGATE during Task 1 and note in summary).
- `testutils.NeedCouchdb(t)` — same investigation required.
- `testutils.NewSetup(t, t.Name())` — same.

If any of these helpers take `*testing.T` strictly, we CANNOT simply pass `tb`. In that case:
  - Option 1: Type-assert inside `newWebdavTestEnv` when the helper requires `*testing.T` — only works when called from a `*testing.T` caller. For `*testing.B` callers, this assertion fails. Solution: introduce a branched path that handles both, OR confirm the downstream helpers already accept `testing.TB` (they should, per established cozy-stack conventions — many use `testing.TB`).
  - Option 2 (preferred if type assertions needed): In those lines, use a runtime type switch:
    ```go
    switch v := tb.(type) {
    case *testing.T: testutils.NewSetup(v, v.Name())
    case *testing.B: testutils.NewSetup(v, v.Name())  // if NewSetup accepts *testing.B
    }
    ```
  - Preferred approach: just pass `tb` directly; if the build fails, fall back to type-switch only on the offending line. Keep the rest of the function body unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen newWebdavTestEnv signature from *testing.T to testing.TB</name>
  <files>web/webdav/testutil_test.go</files>
  <read_first>
    - web/webdav/testutil_test.go (full file, 67 lines)
    - tests/testutils/test_utils.go (inspect signatures of `NewSetup`, `GetTestInstance`, `NeedCouchdb`, `CreateTestClient`, `GetTestClient`, `GetTestServer` — determine whether each already accepts `testing.TB` or requires `*testing.T`)
    - pkg/config/config/config.go (inspect `UseTestFile` signature — does it accept `testing.TB`?)
  </read_first>
  <action>
    In `web/webdav/testutil_test.go`:

    1. Change line 33 from:
       ```go
       func newWebdavTestEnv(t *testing.T, overrideRoutes func(*echo.Group)) *webdavTestEnv {
       ```
       to:
       ```go
       func newWebdavTestEnv(tb testing.TB, overrideRoutes func(*echo.Group)) *webdavTestEnv {
       ```

    2. Rename every reference to `t` inside the function body (except on lines 35-36 which are deleted in Task 2) to `tb`. Specifically:
       - `t.Helper()` → `tb.Helper()`
       - `config.UseTestFile(t)` → `config.UseTestFile(tb)` (if `UseTestFile` accepts `testing.TB`; otherwise see fallback below)
       - `testutils.NeedCouchdb(t)` → `testutils.NeedCouchdb(tb)` (same caveat)
       - `testutils.NewSetup(t, t.Name())` → `testutils.NewSetup(tb, tb.Name())` (same caveat)
       - `t.TempDir()` → `tb.TempDir()`
       - `testutils.CreateTestClient(t, ts.URL)` → `testutils.CreateTestClient(tb, ts.URL)` (same caveat)
       - `t.Cleanup(ts.Close)` → `tb.Cleanup(ts.Close)`

    3. Build. If any downstream helper strictly requires `*testing.T`, use the following minimal shim for JUST that call:
       ```go
       if tt, ok := tb.(*testing.T); ok {
           tt.Helper()
       }
       // or for helpers that strictly need *testing.T:
       switch v := tb.(type) {
       case *testing.T:
           testutils.SomeHelperThatWantsT(v)
       case *testing.B:
           testutils.SomeHelperThatWantsT(v.ReportAllocs) // adapt
       }
       ```
       Only use the shim on the specific call site that fails to compile — keep the rest of the function clean using `tb`.

    4. Do NOT change the function name. Do NOT change the return type. Do NOT touch any callers in *_test.go files — they pass `t` (a `*testing.T`), which is assignable to `testing.TB` implicitly at call sites.

    5. Preserve the exact line doing the DEBT-01 env-var set (`t.Setenv("COZY_DISABLE_AV_TRIGGER", "1")`) — if plan 04-01 landed first it will be `t.Setenv(...)` and needs to become `tb.Setenv(...)`. If 04-01 landed second, it will be added with `tb.Setenv(...)` directly (coordinate via git merge — whichever plan lands second adjusts the variable name). If a merge conflict arises, the correct final state has `tb.Setenv("COZY_DISABLE_AV_TRIGGER", "1")`.
  </action>
  <verify>
    <automated>go build ./web/webdav/... &amp;&amp; go vet ./web/webdav/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'func newWebdavTestEnv' web/webdav/testutil_test.go` shows `func newWebdavTestEnv(tb testing.TB, overrideRoutes func(*echo.Group)) *webdavTestEnv {`
    - `grep -cn '\\bt\\.' web/webdav/testutil_test.go` returns 0 matches for `t.` as a receiver inside the function body (either 0 or only matches inside comments — manually verify no remaining `t.Helper`, `t.Cleanup`, `t.Name`, `t.TempDir` identifiers)
    - `grep -cn '\\btb\\.' web/webdav/testutil_test.go` returns at least 5 (Helper, Name, TempDir, Cleanup, and at least one more)
    - `go build ./web/webdav/...` exits 0
    - `go vet ./web/webdav/...` exits 0
    - `git diff --stat web/webdav/testutil_test.go` shows only this one file changed (no caller files edited)
  </acceptance_criteria>
  <done>`newWebdavTestEnv` accepts `testing.TB`. All 13 existing `*testing.T` callers compile unchanged because `*testing.T` implements `testing.TB`.</done>
</task>

<task type="auto">
  <name>Task 2: Remove blanket testing.Short() skip from newWebdavTestEnv</name>
  <files>web/webdav/testutil_test.go</files>
  <read_first>
    - web/webdav/testutil_test.go (current state after Task 1)
    - .planning/phases/04-prerequisites-and-instrumentation/04-CONTEXT.md section "testing.Short() blanket skip" (rationale)
  </read_first>
  <action>
    In `web/webdav/testutil_test.go`, delete the following 3 lines (currently lines 35-37 in the original file; may have shifted by Task 1 but the block is identifiable):

    ```go
    if testing.Short() {
        t.Skip("webdav integration tests require a cozy test instance")
    }
    ```

    (After Task 1 widening, the receiver name is `tb`, so the line would be `tb.Skip(...)` — delete it regardless of receiver name.)

    The remaining guard `testutils.NeedCouchdb(tb)` (currently line 42 before this deletion, will shift up) is the proper skip mechanism: it checks `COZY_COUCHDB_URL` and skips if absent. This correctly handles the "no infra available" case without blocking `-short` CI runs.

    Do NOT remove or alter the `testutils.NeedCouchdb(tb)` call. Do NOT remove the `tb.Helper()` call. Do NOT touch `config.UseTestFile(tb)`. Only delete the 3-line `if testing.Short() { tb.Skip(...) }` block.
  </action>
  <verify>
    <automated>grep -c 'testing.Short()' web/webdav/testutil_test.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'testing.Short()' web/webdav/testutil_test.go` returns exactly `0`
    - `grep -c 'NeedCouchdb' web/webdav/testutil_test.go` returns `1` (the proper guard is preserved)
    - `grep -c 'tb.Helper()' web/webdav/testutil_test.go` returns `1` (tb.Helper preserved)
    - `go build ./web/webdav/...` exits 0
    - `go vet ./web/webdav/...` exits 0
  </acceptance_criteria>
  <done>Blanket `testing.Short()` skip removed. `NeedCouchdb` remains as the sole gate for "no CouchDB available".</done>
</task>

<task type="auto">
  <name>Task 3: Verify full suite passes with both changes</name>
  <files></files>
  <read_first>
    - web/webdav/testutil_test.go (post-Task 1 + Task 2 state)
  </read_first>
  <action>
    Run the full WebDAV test suite to confirm no regression:

    ```
    COZY_COUCHDB_URL=http://admin:password@localhost:5984/ go test ./web/webdav/... -count=1 -timeout 5m
    ```

    All tests must pass. Then verify the `-short` behaviour works as expected:

    ```
    # With CouchDB URL set AND -short flag — tests that opt into testing.Short should skip,
    # but tests that don't opt in should still run (because the blanket skip is gone).
    COZY_COUCHDB_URL=http://admin:password@localhost:5984/ go test ./web/webdav/... -short -count=1 -timeout 5m
    ```

    Since no current Phase 1-3 test opts into `testing.Short()` (they were designed around the blanket skip), the `-short` run should execute the same set of tests as the default run. That is the expected state for Phase 4. Phase 5's LARGE tests will add their own `if testing.Short() { t.Skip }` guards.

    If any test fails under the default run, investigate whether the failure is caused by the widening (unlikely — `testing.TB` is a superset) or by the skip removal (also unlikely — the current tests don't depend on being skipped). Fix or roll back. Do NOT mark the task done if tests fail.
  </action>
  <verify>
    <automated>COZY_COUCHDB_URL=http://admin:password@localhost:5984/ go test ./web/webdav/... -count=1 -timeout 5m</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./web/webdav/... -count=1 -timeout 5m` (with COZY_COUCHDB_URL set) exits 0
    - `go test ./web/webdav/... -short -count=1 -timeout 5m` (with COZY_COUCHDB_URL set) exits 0 (same tests as default, since no test currently opts into -short)
    - `go vet ./web/webdav/...` exits 0
    - `git diff --stat` shows exactly 1 file changed in this plan: `web/webdav/testutil_test.go`
  </acceptance_criteria>
  <done>All WebDAV tests pass after the widening + blanket skip removal. DEBT-02 closed and the infrastructure is ready for Phase 8 CI-03's `-short` flag in `go-tests.yml`.</done>
</task>

</tasks>

<verification>
- `newWebdavTestEnv` accepts `testing.TB` (verified by signature grep)
- Blanket `testing.Short()` skip is removed (verified by grep count = 0)
- `NeedCouchdb(tb)` remains the proper guard
- Full WebDAV suite passes under `-count=1`
- No caller file modified (only `testutil_test.go`)
</verification>

<success_criteria>
DEBT-02 closed: future Phase 5 benchmarks can reuse the test environment setup via `*testing.B`. The `-short` flag will correctly gate only heavy tests that opt in (starting Phase 5).
</success_criteria>

<output>
After completion, create `.planning/phases/04-prerequisites-and-instrumentation/04-02-SUMMARY.md` recording:
- Final signature of `newWebdavTestEnv`
- Line count delta for `testutil_test.go`
- Whether any downstream helper required a type-switch shim (and if so, which one)
- Confirmation that blanket skip was removed and `NeedCouchdb` remains
</output>
