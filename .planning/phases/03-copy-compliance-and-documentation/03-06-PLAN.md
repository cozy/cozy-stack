---
phase: 03-copy-compliance-and-documentation
plan: 06
type: execute
wave: 3
depends_on: ["03-02", "03-04"]
files_modified:
  - web/webdav/copy.go
  - web/webdav/copy_test.go
  - web/webdav/move.go
  - web/webdav/move_test.go
autonomous: true
requirements: [TEST-06, COPY-01, COPY-02, COPY-03, TEST-07]
must_haves:
  truths:
    - "The litmus `copymove` suite runs clean (zero FAILED tests) against both /dav/files/ and /remote.php/webdav/"
    - "All 13 copymove tests (begin, copy_init, copy_simple, copy_overwrite, copy_nodestcoll, copy_cleanup, copy_coll, copy_shallow, move, move_coll, move_cleanup, finish) pass on both routes"
    - "Plan 03-02's COPY directory implementation is validated end-to-end by a real external compliance tester"
    - "Any gap is closed with RED→GREEN commits (TEST-07 discipline)"
  artifacts:
    - path: ".planning/phases/03-copy-compliance-and-documentation/03-06-INVENTORY.md"
      provides: "First-run litmus copymove results with per-test pass/fail + root-cause hypotheses"
  key_links:
    - from: "scripts/webdav-litmus.sh"
      to: "litmus copymove suite"
      via: "LITMUS_TESTS=copymove"
      pattern: "copymove"
---

<objective>
Run the litmus `copymove` suite against both routes and close any gap until both routes are clean. This is
the REAL compliance validation for the COPY implementation landed in plans 03-01 and 03-02. The suite's
13 tests map directly to COPY-01/02/03 requirements — this plan transitively confirms them all at the
external compliance layer.

Suite tests (from research §1): begin, copy_init, copy_simple, copy_overwrite, copy_nodestcoll, copy_cleanup,
copy_coll (Depth:infinity), copy_shallow (Depth:0), move, move_coll, move_cleanup, finish.

Purpose: Close the copymove portion of TEST-06. Because plan 03-02 already passes exhaustive unit tests for
file and directory COPY, the expected outcome is that litmus is clean on the first run. If not, each failure
is a genuine wire-level discrepancy that must be fixed with RED→GREEN commits.

Output:
- `03-06-INVENTORY.md` with first-run results and any fixes
- `03-06-SUMMARY.md` with final pass state
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-copy-compliance-and-documentation/03-RESEARCH.md §1 (copymove test names), §2 (RFC 4918 §9.8)
@web/webdav/copy.go
@web/webdav/move.go
@web/webdav/copy_test.go
@scripts/webdav-litmus.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run copymove suite against both routes and inventory failures</name>
  <files>.planning/phases/03-copy-compliance-and-documentation/03-06-INVENTORY.md</files>
  <read_first>
    - scripts/webdav-litmus.sh (confirm LITMUS_TESTS filter works)
    - web/webdav/copy.go (the handler being validated)
    - web/webdav/move.go (the Phase 2 MOVE handler also exercised by this suite)
    - .planning/phases/03-copy-compliance-and-documentation/03-RESEARCH.md §1 (expected behavior per test)
  </read_first>
  <action>
    Run:
    ```bash
    LITMUS_TESTS="copymove" scripts/webdav-litmus.sh 2>&1 | tee /tmp/litmus-copymove-output.log
    ```

    Create `.planning/phases/03-copy-compliance-and-documentation/03-06-INVENTORY.md` documenting:
    - Date/time, cozy-stack commit, litmus version
    - Route 1 (`/dav/files/`): pass/fail count, list of failing tests
    - Route 2 (`/remote.php/webdav/`): pass/fail count, list of failing tests
    - For each failure, which COPY or MOVE contract is at issue (map to COPY-01/02/03, MOVE-01..05)
    - Root-cause hypothesis (copy.go line, move.go line, write_helpers.go, etc.)

    Commit message: `docs(03-06): inventory litmus copymove suite first-run results`
  </action>
  <verify>
    <automated>test -f .planning/phases/03-copy-compliance-and-documentation/03-06-INVENTORY.md && grep -q "dav/files\|remote.php/webdav" .planning/phases/03-copy-compliance-and-documentation/03-06-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/03-copy-compliance-and-documentation/03-06-INVENTORY.md` exists
    - Inventory lists both routes explicitly
    - Inventory names each failed test OR explicitly states "both routes clean, zero failures"
    - `git log -1 --format=%s` contains `03-06`
  </acceptance_criteria>
  <done>Inventory captures first-run state of the copymove suite on both routes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN per gap from inventory until copymove is clean</name>
  <files>web/webdav/copy.go, web/webdav/copy_test.go, web/webdav/move.go, web/webdav/move_test.go</files>
  <read_first>
    - .planning/phases/03-copy-compliance-and-documentation/03-06-INVENTORY.md (from Task 1)
    - web/webdav/copy.go (handler to fix)
    - web/webdav/copy_test.go (extend with reproducer tests — do not rewrite)
    - web/webdav/move.go / move_test.go (if a MOVE test fails, fix there instead)
  </read_first>
  <behavior>
    For each failure in the inventory:
    1. Write a Go unit test reproducing the wire-level gap (test name references the litmus test,
       e.g., `TestCopy_LitmusCopymoveCopyOverwrite`).
    2. Confirm RED: `go test -run <NewTestName> ./web/webdav/...` fails.
    3. Commit: `test(03-06): RED — reproduce litmus copymove/<test_name>`
    4. Fix the handler minimally.
    5. Confirm GREEN at the unit layer.
    6. Commit: `fix(03-06): GREEN — address copymove/<test_name>`
    7. Re-run `LITMUS_TESTS="copymove" scripts/webdav-litmus.sh` — confirm the specific litmus test now passes.
    8. Iterate to next failure.

    Expected likely gaps (hypotheses — verify empirically):
    - `copy_overwrite` may send `Overwrite: F` for an existing destination and expect 412 — already handled
      by plan 03-01 Task 2, should pass.
    - `copy_nodestcoll` sends COPY to a destination whose parent collection does not exist — expects 409.
      Already handled.
    - `copy_coll` with Depth:infinity tests recursive directory copy — plan 03-02 GREEN.
    - `copy_shallow` with Depth:0 tests empty-container copy — plan 03-02 GREEN.

    If first-run inventory shows zero failures, this task is a no-op.
  </behavior>
  <action>
    Iterate through inventory failures exactly as described in Task 2 of plan 03-05, but scoped to
    the copymove suite and the copy.go/move.go handlers. Commits follow the `test(03-06): RED` / `fix(03-06): GREEN` pattern.

    After all failures are closed, re-run the full suite to verify zero failures on both routes:
    ```bash
    LITMUS_TESTS="copymove" scripts/webdav-litmus.sh
    ```
  </action>
  <verify>
    <automated>LITMUS_TESTS="copymove" scripts/webdav-litmus.sh</automated>
  </verify>
  <acceptance_criteria>
    - `LITMUS_TESTS="copymove" scripts/webdav-litmus.sh` exits 0
    - Litmus output contains `0 failed` for BOTH `/dav/files/` AND `/remote.php/webdav/` copymove runs
    - `go test -p 1 -timeout 5m ./web/webdav/...` exits 0 (all unit tests still green)
    - If any fixes were made, the git log shows paired RED/GREEN commits
    - `.github/workflows/` unchanged: `git diff origin/master -- .github/workflows/ | wc -l` returns `0`
    - `03-06-INVENTORY.md` has a "Final state" section marking all failures as fixed or noting zero failures
  </acceptance_criteria>
  <done>
    The copymove suite passes cleanly on both routes. COPY-01/02/03 are transitively validated at the
    external compliance layer.
  </done>
</task>

</tasks>

<verification>
- `LITMUS_TESTS="copymove" scripts/webdav-litmus.sh` exits 0
- `go test -p 1 -timeout 5m ./web/webdav/...` exits 0
- No CI workflow changes
</verification>

<success_criteria>
- TEST-06 closed for copymove suite
- COPY-01, COPY-02, COPY-03 transitively validated by litmus
- Anti-regression gate for the Nextcloud route still holds
</success_criteria>

<output>
After completion, create `.planning/phases/03-copy-compliance-and-documentation/03-06-SUMMARY.md`.
</output>
