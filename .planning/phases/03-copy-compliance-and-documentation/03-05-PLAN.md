---
phase: 03-copy-compliance-and-documentation
plan: 05
type: execute
wave: 2
depends_on: ["03-04"]
files_modified:
  - web/webdav/mkcol.go
  - web/webdav/mkcol_test.go
  - web/webdav/propfind.go
  - web/webdav/propfind_test.go
  - web/webdav/delete.go
  - web/webdav/delete_test.go
  - web/webdav/handlers.go
autonomous: true
requirements: [TEST-06, TEST-07]
must_haves:
  truths:
    - "The litmus `basic` suite runs clean (zero FAILED tests) against both /dav/files/ and /remote.php/webdav/"
    - "Any gap revealed by the first `basic` run is addressed via a RED test → GREEN handler fix → commit, per TDD discipline"
    - "mkcol_with_body returns 415 Unsupported Media Type (already implemented in Phase 2 plan 02-03 — verify)"
  artifacts:
    - path: "scripts/webdav-litmus.sh"
      provides: "Script from plan 03-04 — used here with LITMUS_TESTS=basic"
    - path: "web/webdav/*.go"
      provides: "Any targeted gap fixes — exact files depend on first-run findings"
  key_links:
    - from: "scripts/webdav-litmus.sh"
      to: "litmus basic suite"
      via: "LITMUS_TESTS=basic"
      pattern: "LITMUS_TESTS.*basic"
---

<objective>
Run the litmus `basic` suite (16 tests: init, begin, options, put_get, put_get_utf8_segment, put_no_parent,
mkcol_over_plain, delete, delete_null, delete_fragment, mkcol, mkcol_again, delete_coll, mkcol_no_parent,
mkcol_with_body, finish) against BOTH `/dav/files/` and `/remote.php/webdav/`. Fix every actual failure with
RED→GREEN commits until both routes are clean.

Research §7 flagged `mkcol_with_body` as a likely gap. **Verification: `web/webdav/mkcol.go` lines 25-37 already
implement the 415 guard** (Phase 2 plan 02-03 added it). This plan should still verify it empirically under litmus —
if the guard is insufficient (e.g., litmus uses chunked encoding that slips past `ContentLength > 0`), fix it.

The plan is DISCOVERY-driven: we do not know ahead of time exactly which tests will fail. The workflow is:
1. Run the suite → inventory failures
2. For each failure, write a RED unit test (or extend an existing one) that reproduces the gap at the Go layer
3. Fix the handler to pass the unit test (GREEN)
4. Re-run the litmus suite → verify the specific failure is gone
5. Repeat until both routes are clean

Purpose: Close part of TEST-06 (basic suite portion). Anti-regression verification of Phase 1 + 2 handlers
under a real external compliance tester.

Output:
- Possibly zero source changes if Phase 1/2 are already compliant
- Possibly one or more commits with test + fix pairs targeting specific failed litmus tests
- `03-05-SUMMARY.md` documenting which tests ran, which failed, which were fixed, which still pass cleanly
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-copy-compliance-and-documentation/03-RESEARCH.md §1 (basic suite test names), §7 pitfall J (mkcol_with_body)
@web/webdav/mkcol.go
@web/webdav/propfind.go
@web/webdav/delete.go
@web/webdav/put.go
@web/webdav/get.go
@web/webdav/handlers.go
@scripts/webdav-litmus.sh

<interfaces>
Running the basic suite only:
```bash
LITMUS_TESTS="basic" scripts/webdav-litmus.sh
```

The suite output format (from research §1):
```
<- summary for 'basic': of 16 tests run: 16 passed, 0 failed. 100.0%
```
or with failures:
```
 4. put_get............... FAIL (expected 201, got 204)
<- summary for 'basic': of 16 tests run: 15 passed, 1 failed. 93.7%
```

Exit code: non-zero if any test failed.

Phase 1/2 handler files touched by basic suite:
- OPTIONS → web/webdav/handlers.go:handleOptions
- PUT → web/webdav/put.go:handlePut
- GET → web/webdav/get.go:handleGet
- MKCOL → web/webdav/mkcol.go:handleMkcol (ALREADY has 415 guard for mkcol_with_body)
- DELETE → web/webdav/delete.go:handleDelete
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run basic suite against both routes and inventory failures</name>
  <files>.planning/phases/03-copy-compliance-and-documentation/03-05-INVENTORY.md</files>
  <read_first>
    - scripts/webdav-litmus.sh (the orchestration script from plan 03-04 — confirm it accepts LITMUS_TESTS)
    - web/webdav/mkcol.go (confirm the 415 guard is present at lines 25-37)
    - .planning/phases/03-copy-compliance-and-documentation/03-RESEARCH.md §1 (16 basic test names)
  </read_first>
  <action>
    Ensure a cozy-stack is running locally at `localhost:8080`. If not, instruct the user via explicit console
    message and pause (or start one via `make run` / equivalent — check the repo's README for the canonical
    invocation). Document the required preconditions in the inventory file.

    Run:
    ```bash
    LITMUS_TESTS="basic" scripts/webdav-litmus.sh 2>&1 | tee /tmp/litmus-basic-output.log
    ```

    Capture the exit code. Parse the log for lines matching the pattern `FAIL` or
    `summary for 'basic': of N tests run: M passed, K failed` where `K > 0`.

    Create a new file `.planning/phases/03-copy-compliance-and-documentation/03-05-INVENTORY.md` documenting:
    - Date/time of run
    - cozy-stack version/commit
    - litmus version (`litmus --version` or `dpkg -l litmus`)
    - Route 1 (`/dav/files/`): pass/fail count, list of failing tests with expected vs actual
    - Route 2 (`/remote.php/webdav/`): pass/fail count, list of failing tests with expected vs actual
    - For each failure, a root-cause hypothesis (which handler file likely needs fixing)

    If BOTH routes pass clean (16/16), the inventory still gets created and the plan's later tasks are no-ops
    (just commit the inventory and move to plan 03-06).

    If failures exist, each failure becomes an entry in the inventory with a hypothesis, and Task 2 is
    triggered for each.

    Commit message: `docs(03-05): inventory litmus basic suite first-run results`
  </action>
  <verify>
    <automated>test -f .planning/phases/03-copy-compliance-and-documentation/03-05-INVENTORY.md && grep -q "Route 1" .planning/phases/03-copy-compliance-and-documentation/03-05-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/03-copy-compliance-and-documentation/03-05-INVENTORY.md`
    - `grep -q "dav/files" .planning/phases/03-copy-compliance-and-documentation/03-05-INVENTORY.md`
    - `grep -q "remote.php/webdav" .planning/phases/03-copy-compliance-and-documentation/03-05-INVENTORY.md`
    - `grep -q "passed\|failed" .planning/phases/03-copy-compliance-and-documentation/03-05-INVENTORY.md`
    - Either (a) inventory lists zero failures AND a line like "both routes clean" OR
      (b) inventory lists each failed test name with a hypothesis
    - `git log -1 --format=%s` contains `03-05` and mentions inventory or basic
  </acceptance_criteria>
  <done>
    The inventory file exists, captures the first-run state of the basic suite on both routes, and provides
    enough structure for Task 2 to act on any failures.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN per gap from inventory (iterative until basic is clean)</name>
  <files>web/webdav/*.go, web/webdav/*_test.go</files>
  <read_first>
    - .planning/phases/03-copy-compliance-and-documentation/03-05-INVENTORY.md (from Task 1)
    - The specific handler file flagged by each inventory entry
    - The corresponding `_test.go` file (extend, do not rewrite existing tests)
  </read_first>
  <behavior>
    For each failure in the inventory, the workflow is strictly TDD:
    1. Write a Go unit test that reproduces the failure at the handler layer. The test must FAIL before the fix.
    2. Commit: `test(03-05): RED — reproduce litmus basic/<test_name> failure`
    3. Fix the handler. Commit: `fix(03-05): GREEN — address litmus basic/<test_name>`
    4. Re-run `LITMUS_TESTS="basic" scripts/webdav-litmus.sh` and confirm the specific test now passes.
    5. Repeat for next failure.

    If the inventory shows zero failures, this task is a no-op — mark it `done` in the plan summary and move on.

    Examples of likely failures and their TDD response:
    - `mkcol_with_body` fails because chunked body slips past `ContentLength > 0`: write a test that sends
      MKCOL with `Transfer-Encoding: chunked` and a non-empty chunked body, assert 415. Fix `mkcol.go` to
      also read the body buffer when ContentLength is -1 (already does this at lines 31-37 — verify it works).
    - `put_no_parent` fails because the handler returns 404 instead of 409: write a unit test that PUTs
      to `/nonexistent/file.txt`, assert 409. Fix the PUT handler to distinguish "parent missing" from
      "resource missing".
    - `put_get_utf8_segment` fails because path normalization strips UTF-8: write a unit test that seeds
      then GETs a file with a UTF-8 filename, fix `davPathToVFSPath` if needed.

    **Scope guard:** This task ONLY fixes failures in the `basic` suite. Failures in other suites are handled
    by plans 03-06..03-08. If a fix inadvertently touches functionality exercised by copymove/props/http,
    confirm the other plans' suites remain green (or defer regression analysis to those plans).
  </behavior>
  <action>
    Iterate over the inventory failures. For each:

    1. Read the handler file cited in the root-cause hypothesis.
    2. Write/extend a table-driven test in the corresponding `_test.go` file. The test name should mention
       the litmus test name (e.g., `TestMkcol_LitmusBasicMkcolWithBodyChunked`).
    3. Run `go test -p 1 -timeout 5m -run <NewTestName> ./web/webdav/...` — confirm RED.
    4. Commit RED.
    5. Fix the handler. Keep the fix minimal — do NOT refactor unrelated code.
    6. Run the unit test again — confirm GREEN.
    7. Commit GREEN.
    8. Re-run `LITMUS_TESTS="basic" scripts/webdav-litmus.sh` on both routes — confirm the SPECIFIC litmus
       test that previously failed now passes. Do not proceed to the next failure until this one is closed.

    After all failures are addressed, do a final full-suite run to confirm 16/16 on both routes.

    If the inventory shows zero failures from Task 1, this task creates no commits — skip to the summary.

    Commit messages follow the pattern:
    - `test(03-05): RED — reproduce <litmus_test> failure`
    - `fix(03-05): GREEN — address <litmus_test> failure in <handler_file>`
  </action>
  <verify>
    <automated>LITMUS_TESTS="basic" scripts/webdav-litmus.sh</automated>
  </verify>
  <acceptance_criteria>
    - `LITMUS_TESTS="basic" scripts/webdav-litmus.sh` exits 0 (both routes clean)
    - Litmus output contains `of 16 tests run:` and `0 failed` for BOTH `/dav/files/` and `/remote.php/webdav/`
    - `go test -p 1 -timeout 5m ./web/webdav/...` exits 0 (all unit tests still pass)
    - If any source changes were made, the git log shows a RED commit and a GREEN commit for each fix, in that order
    - The 03-05-INVENTORY.md file has been updated (or a follow-up section appended) marking each failure as FIXED
    - `.github/workflows/` is unchanged: `git diff origin/master -- .github/workflows/ | wc -l` returns `0`
  </acceptance_criteria>
  <done>
    Litmus `basic` suite passes cleanly on both routes. Any fixes were made via disciplined RED→GREEN commits.
    The inventory file reflects final state.
  </done>
</task>

</tasks>

<verification>
- `LITMUS_TESTS="basic" scripts/webdav-litmus.sh` exits 0
- `go test -p 1 -timeout 5m ./web/webdav/...` exits 0
- No regressions in copymove/props/http suites (not verified by this plan; follow-up plans handle them)
- `.github/workflows/` untouched
</verification>

<success_criteria>
- TEST-06 partially closed (basic suite portion)
- Phase 1/2 handlers confirmed to produce compliant wire behavior for the basic suite
- Any hidden gaps surfaced by litmus are closed with test-first commits (TEST-07 discipline preserved)
</success_criteria>

<output>
After completion, append to `.planning/phases/03-copy-compliance-and-documentation/03-05-INVENTORY.md`
a "Final state" section, then create `.planning/phases/03-copy-compliance-and-documentation/03-05-SUMMARY.md`
summarizing the gaps (if any), the fixes, and confirming both routes pass cleanly.
</output>
