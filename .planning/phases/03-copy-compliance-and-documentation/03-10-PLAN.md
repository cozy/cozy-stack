---
phase: 03-copy-compliance-and-documentation
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [COPY-01, COPY-02, COPY-03, DOC-01, DOC-02, DOC-03, DOC-04, TEST-05, TEST-06, TEST-07]
must_haves:
  truths:
    - "REQUIREMENTS.md reflects the explicit Phase 3 scope reduction: iOS/iPadOS Files app validation is deferred to v1.1"
    - "The iOS Files line in REQUIREMENTS.md no longer reads as a v1 blocking criterion; it reads as 'best-effort, covered indirectly by litmus Class 1 strict, formal validation deferred to v1.1'"
    - "All Phase 3 requirements (COPY-01..03, DOC-01..04, TEST-05..07) are transitioned to the appropriate status in the traceability table"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated Phase 3 requirement statuses + iOS Files deferral note"
      contains: "best-effort\\|deferred to v1.1"
  key_links: []
---

<objective>
Update `.planning/REQUIREMENTS.md` to reflect two things:

1. **The explicit Phase 3 scope reduction** for iOS/iPadOS Files app validation. This is a conscious user
   decision captured in CONTEXT.md — it must NOT be a silent omission. The new wording makes clear that
   v1 does not formally validate iOS Files manually and that this validation is explicitly deferred to v1.1.

2. **Phase 3 requirement status transitions** as each underlying plan lands. This plan is the sole mutator
   of REQUIREMENTS.md in Phase 3 — keeping all status bookkeeping in one place prevents merge noise.

This is a documentation-only plan, no code. It is in Wave 4 (the same wave as the docs plan 03-09) because
it should land AFTER the implementation plans have completed (so the status transitions reflect reality).
It has no code dependencies; `depends_on` is empty because the bookkeeping can run in parallel with 03-09
and does not need the litmus suite results to have landed — it updates status based on plan completion,
not on runtime verification.

Purpose: Mandatory per CONTEXT.md: "⚠️ réduction de scope explicite... le planner DOIT updater REQUIREMENTS.md
pour refléter ce changement. Ce n'est pas un simple oubli, c'est une décision consciente". Also closes
the per-requirement bookkeeping for Phase 3.

Output:
- `.planning/REQUIREMENTS.md` with an updated TEST-05 wording, an updated Traceability table, and any
  explicit note about iOS Files deferral.
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-copy-compliance-and-documentation/03-CONTEXT.md

<interfaces>
Current REQUIREMENTS.md structure (from the file read):
- Core value paragraph (in French — do NOT translate)
- v1 Requirements section with sub-sections (Routing, Auth, Read, Write, Move & Copy, Security, Documentation, Testing)
- v2 Requirements section
- Out of Scope table
- Traceability table (all 53 v1 requirements with Phase and Status columns)

Relevant current lines (from the file read earlier in this session):
```
- [ ] **TEST-05**: Tests de comportement : scénarios OnlyOffice mobile et iOS Files (open → read → write → save)
```

The Traceability table currently marks COPY-01..03, DOC-01..04, TEST-05..07 as `Pending`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TEST-05 wording to reflect iOS Files deferral to v1.1</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — identify the TEST-05 line and the Traceability table)
    - .planning/phases/03-copy-compliance-and-documentation/03-CONTEXT.md (the explicit scope-reduction text)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md`. Locate the `### Testing (TDD strict)` section. Replace the TEST-05 line:

    FROM:
    ```
    - [ ] **TEST-05**: Tests de comportement : scénarios OnlyOffice mobile et iOS Files (open → read → write → save)
    ```

    TO:
    ```
    - [ ] **TEST-05**: Tests de comportement WebDAV — couverture par transitivité via litmus Class 1 strict + E2E gowebdav. OnlyOffice mobile : validation manuelle reportée jusqu'à fix client v9.3.2+. **iOS/iPadOS Files app : validation manuelle déférée à v1.1** — best-effort en v1, couvert indirectement par litmus Class 1 strict sur les deux routes (`/dav/files/` et `/remote.php/webdav/`).
    ```

    This replacement preserves the REQUIREMENTS.md French convention (see CONTEXT.md: "PROJECT.md/REQUIREMENTS.md
    restent en français").

    Also ADD a new subsection `### Scope reductions (Phase 3)` BELOW the `### Testing (TDD strict)` section
    (before the `## v2 Requirements` heading), containing:
    ```
    ### Scope reductions (Phase 3)

    Réductions de scope explicites décidées durant `/gsd:discuss-phase` pour la Phase 3 :

    - **iOS/iPadOS Files app validation manuelle → v1.1** — Décision consciente (non un oubli). La Phase 3
      ne bloque plus la release v1 sur un test manuel iOS Files. La compatibilité est couverte indirectement
      par la conformité litmus Class 1 strict sur les deux routes WebDAV. La vérification manuelle reprend
      en v1.1 quand on aura le temps et/ou un device de test dédié.
    - **CI intégration litmus → v1.1** — `make test-litmus` est exécutable uniquement localement en Phase 3.
      L'intégration CI (`.github/workflows/system-tests.yml`) est reportée post-v1.
    - **Test manuel OnlyOffice mobile → dépendant du fix client v9.3.2+** — bloqué par un bug client
      (`LoginComponent null`), pas par le serveur. Reprend quand le fix upstream est disponible.
    ```

    Do NOT translate existing French content to English. Do NOT reorder existing sections. Do NOT touch
    requirements outside Phase 3.

    Commit message: `docs(03-10): update REQUIREMENTS.md — iOS Files deferral and Phase 3 scope reductions`
  </action>
  <verify>
    <automated>grep -q "v1.1\|v1\\.1" .planning/REQUIREMENTS.md && grep -qi "iOS.*déférée\|iOS.*deferred\|best.effort" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qi "best.effort" .planning/REQUIREMENTS.md`
    - `grep -q "v1.1" .planning/REQUIREMENTS.md`
    - `grep -qi "iOS" .planning/REQUIREMENTS.md`
    - `grep -qi "déférée\|déférée à v1.1\|deferred to v1.1\|reportée" .planning/REQUIREMENTS.md`
    - `grep -q "### Scope reductions" .planning/REQUIREMENTS.md`
    - `grep -q "litmus Class 1" .planning/REQUIREMENTS.md`
    - The OLD exact TEST-05 line is gone:
      `! grep -q '^- \[ \] \*\*TEST-05\*\*: Tests de comportement : scénarios OnlyOffice mobile et iOS Files (open → read → write → save)$' .planning/REQUIREMENTS.md`
    - The new TEST-05 line exists and mentions "transitivité" OR "best-effort" OR "v1.1"
    - `grep -q "CI intégration litmus\|.github/workflows" .planning/REQUIREMENTS.md` (CI deferral documented)
    - Out-of-scope and v2 sections are UNCHANGED:
      `grep -c "LOCK/UNLOCK v1" .planning/REQUIREMENTS.md` returns at least 1 (existing out-of-scope entry preserved)
    - `git diff .planning/REQUIREMENTS.md` shows ONLY additions to Testing section and a new Scope-reductions
      section — no deletions outside the TEST-05 line replacement
    - `git log -1 --format=%s` contains `03-10` and mentions iOS or deferral or scope
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md reflects the explicit iOS Files deferral and the Phase 3 scope reductions. The wording
    is unambiguous and French (repo convention for .planning/).
  </done>
</task>

</tasks>

<verification>
- `grep -qi "best.effort" .planning/REQUIREMENTS.md`
- `grep -q "v1.1" .planning/REQUIREMENTS.md`
- `grep -q "### Scope reductions" .planning/REQUIREMENTS.md`
- No code changes (file list: only `.planning/REQUIREMENTS.md`)
</verification>

<success_criteria>
- REQUIREMENTS.md reflects the Phase 3 scope reduction (iOS Files v1.1 deferral) per CONTEXT.md
- Reviewers and future phases know the deferral is a conscious decision, not an oversight
- CI litmus deferral also documented in-place
</success_criteria>

<output>
After completion, create `.planning/phases/03-copy-compliance-and-documentation/03-10-SUMMARY.md`
with a short note about the scope reduction and its rationale, citing CONTEXT.md.
</output>
