---
phase: 03-copy-compliance-and-documentation
plan: 08
type: execute
wave: 2
depends_on: ["03-04"]
files_modified:
  - web/webdav/put.go
  - web/webdav/put_test.go
autonomous: true
requirements: [TEST-06, TEST-07]
must_haves:
  truths:
    - "The litmus `http` suite runs clean (zero FAILED tests) against both /dav/files/ and /remote.php/webdav/"
    - "The server correctly handles Expect: 100-continue on PUT requests"
    - "Any gap is closed with RED→GREEN commits"
  artifacts:
    - path: ".planning/phases/03-copy-compliance-and-documentation/03-08-INVENTORY.md"
      provides: "First-run litmus http results"
  key_links:
    - from: "scripts/webdav-litmus.sh"
      to: "litmus http suite"
      via: "LITMUS_TESTS=http"
      pattern: "http"
---

<objective>
Run the litmus `http` suite (4 tests: init, begin, expect100, finish) against both routes and close any gap.
The only substantive test is `expect100` which sends a PUT with `Expect: 100-continue` header and verifies
the server handles or ignores it correctly.

Per research §1: Go's `net/http` handles `Expect: 100-continue` automatically in the server (it sends
`100 Continue` before reading the request body). Our `handlePut` should pass this without any changes.
The first-run is the empirical confirmation.

Purpose: Close the http portion of TEST-06. Smallest of the five suites — likely zero changes needed.
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-copy-compliance-and-documentation/03-RESEARCH.md §1 (http suite)
@web/webdav/put.go
@scripts/webdav-litmus.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run http suite and fix any gap (likely no-op)</name>
  <files>.planning/phases/03-copy-compliance-and-documentation/03-08-INVENTORY.md, web/webdav/put.go, web/webdav/put_test.go</files>
  <read_first>
    - scripts/webdav-litmus.sh
    - web/webdav/put.go (current PUT handler — confirm it does not pre-read the body before Go's net/http
      has a chance to send the 100-Continue response)
    - .planning/phases/03-copy-compliance-and-documentation/03-RESEARCH.md §1 (http suite description)
  </read_first>
  <action>
    Run:
    ```bash
    LITMUS_TESTS="http" scripts/webdav-litmus.sh 2>&1 | tee /tmp/litmus-http-output.log
    ```

    Create `.planning/phases/03-copy-compliance-and-documentation/03-08-INVENTORY.md` documenting the
    per-route pass/fail counts.

    If both routes pass clean (expected outcome): commit the inventory with the pass state and you are done.

    If `expect100` fails on either route: investigate. The likely cause would be something in our middleware
    chain reading `r.Body` or checking `r.ContentLength` BEFORE the `net/http` server has had a chance to send
    `100 Continue`. The fix is usually to defer any body touch until after the handler acknowledges the
    precondition headers (e.g., after content-type validation).

    For any fix: write a RED unit test in `put_test.go` that uses a `httptest.NewRequest` with the `Expect:
    100-continue` header and a non-empty body, then verify the server responds correctly. Iterate RED→GREEN.

    Commit messages:
    - Always: `docs(03-08): inventory litmus http suite results`
    - If fixes: `test(03-08): RED — reproduce http/expect100 failure` / `fix(03-08): GREEN — handle expect100 on PUT`
  </action>
  <verify>
    <automated>LITMUS_TESTS="http" scripts/webdav-litmus.sh</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/03-copy-compliance-and-documentation/03-08-INVENTORY.md` exists
    - `LITMUS_TESTS="http" scripts/webdav-litmus.sh` exits 0 on both routes
    - `go test -p 1 -timeout 5m ./web/webdav/...` exits 0
    - `.github/workflows/` unchanged
    - If any source changes were made, they include paired RED/GREEN commits
  </acceptance_criteria>
  <done>
    The http suite passes cleanly on both routes.
  </done>
</task>

</tasks>

<verification>
- `LITMUS_TESTS="http" scripts/webdav-litmus.sh` exits 0
- `go test -p 1 -timeout 5m ./web/webdav/...` exits 0
- CI untouched
</verification>

<success_criteria>
- TEST-06 closed for http suite
- Expect: 100-continue behavior validated against real external compliance tester
- Once plans 03-05, 03-06, 03-07, 03-08 all complete, running `scripts/webdav-litmus.sh` with no TESTS filter
  (all 5 suites: basic, copymove, props, http, locks) should exit 0 on both routes, with locks cleanly
  auto-skipping per research §6. TEST-06 is then fully closed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-copy-compliance-and-documentation/03-08-SUMMARY.md`
confirming the http suite passes and noting whether the full `scripts/webdav-litmus.sh` (no filter) passes
both routes — this is the final TEST-06 gate.
</output>
